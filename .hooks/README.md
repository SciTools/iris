# Local pre-commit hooks

Each module in this directory is a local pre-commit hook for Iris, written in the format expected by the pre-commit framework. See [`.pre-commit-config.yaml`](../.pre-commit-config.yaml).

**TODO: review this new framework fully.**
