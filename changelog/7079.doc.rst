:user:`tkknight` updated the voted table that uses datatables to not highlight the
sorted column or row as is uses the incorrect theme color (light). Also updated
the datatables version from 2.3.2 to 2.3.8.
